{
    attributes = (
        {
            columnName = "FANN_COURANTE"; 
            externalType = VARCHAR2; 
            name = fannCourante; 
            valueClassName = NSString; 
            valueType = S; 
            width = 1; 
        }, 
        {
            columnName = "FANN_DEBUT"; 
            externalType = NUMBER; 
            name = fannDebut; 
            valueClassName = NSNumber; 
            valueType = i; 
        }, 
        {
            columnName = "FANN_FIN"; 
            externalType = NUMBER; 
            name = fannFin; 
            valueClassName = NSNumber; 
            valueType = i; 
        }, 
        {
            columnName = "FANN_KEY"; 
            externalType = NUMBER; 
            name = fannKey; 
            valueClassName = NSNumber; 
            valueType = i; 
        }
    ); 
    attributesUsedForLocking = (fannKey, fannDebut, fannFin, fannCourante); 
    className = "org.cocktail.groupescol.serveur.eof.ScolFormationAnnee"; 
    classProperties = (fannDebut, fannFin, fannCourante, fannKey); 
    externalName = "SCOL_FORMATION_ANNEE"; 
    fetchSpecificationDictionary = {}; 
    internalInfo = {
        "_clientClassPropertyNames" = (fannDebut, fannFin, fannCourante, fannKey); 
        "_javaClientClassName" = "org.cocktail.groupescol.client.eof.ScolFormationAnnee"; 
    }; 
    name = ScolFormationAnnee; 
    primaryKeyAttributes = (fannKey); 
}
